# This is a cool project

My project is very cool

## Setup

You ca start the project with the start command

## Building steps

1. Install nodejs: `npm i -g node`
2. Install npx: `npm i npx`
3. Install dependencies: `npm install`